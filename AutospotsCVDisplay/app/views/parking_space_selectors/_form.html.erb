<%= form_for(parking_space_selector) do |f| %>
  <% if parking_space_selector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parking_space_selector.errors.count, "error") %> prohibited this parking_space_selector from being saved:</h2>

      <ul>
      <% parking_space_selector.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class='form-horizontal'>
  <div class="form-group field">
    <%= f.label :content, 'Output', class: 'col-sm-2 control-label'%>
    <%= f.text_area :content, :rows => "3", :readonly => true, class: 'form-control' %>
  </div>

  <!-- <div class="form-group field"> 
     <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div> -->

  <div class="form-group actions">
    <%= f.submit class: 'btn btn-secondary'%>
  </div>
  
</div>
<% end %>
